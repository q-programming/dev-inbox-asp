/**
 * Identity module — user entity, per-user integration credentials (GitHub PAT, ADO PAT/org).
 */
@org.springframework.modulith.ApplicationModule(allowedDependencies = "shared")
package pl.qprogramming.devinbox.identity;
