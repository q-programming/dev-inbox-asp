/**
 * Domain entities and repositories for the identity module — User entity and UserRepository.
 * Accessible to other modules (e.g. security) that declare "identity" in allowedDependencies.
 */
package pl.qprogramming.devinbox.identity.domain;
