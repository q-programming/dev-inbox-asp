/**
 * Exposed as a named interface so other modules can access
 * {@code CookieUtils} and {@code SecurityUtils} via {@code shared::utils}
 * without these utilities being part of the default {@code shared} public API.
 */
@org.springframework.modulith.NamedInterface("utils")
package pl.qprogramming.devinbox.shared.utils;
