/**
 * Security cross-cutting module — JWT filters, encryption service.
 * Does NOT depend on identity or config — those modules depend on security.
 */
@org.springframework.modulith.ApplicationModule
package pl.qprogramming.devinbox.security;
